const express = require("express");
const router = express.Router();
router.get("/saldo", (req, res) => res.json({ message: "Not implemented yet" }));
router.get("/extrato", (req, res) => res.json({ message: "Not implemented yet" }));
module.exports = router;
